# X アルゴリズム 上流変更レポート 2026-09-18

## メタ情報

| 項目 | 値 |
|---|---|
| 対象コミット (NEW) | `c279172eb8a992170f077d4c1d441923ef215d2e` |
| 前回解析コミット (OLD) | `42266f3e2be54d8a3c4d2228aac2e8f8d1c1f034` |
| 解析日 | 2026-09-18 |
| 変更ファイル数 | 30 ファイル（+1491 / -154） |
| 新規コミット | 1 件（`c279172` Open-source X Recommendation Algorithm、CI agent、Fri Sep 18 01:07:14 2026 UTC） |
| ランキング重み（`ScoringWeights` の数値） | **変更なし（23日連続）**。`home-mixer/scorers/` は1行も変更されていない |
| 予測ヘッドの増減 | 配信側 0。学習側で広告の**遅延コンバージョン10ヘッド**を「twin ヘッド」として正式に学習対象化（既定オフ） |
| Thunder リテンション窓 / OON係数(`effective_oon_weight`) / 著者多様性 decay・floor / retrieval Top-K | いずれも変更なし |
| 広告挿入密度・ブランドセーフティ (`home-mixer/ads/`) | **変更なし**（1行も触られていない） |
| `home-mixer/params/param.rs` | **同期タイムスタンプのみ**（`2026-09-16T16:23:12Z` → `2026-09-17T20:46:44Z`）。フィーチャースイッチの既定値は1つも変わっていない |
| 今回の主戦場 | **(a) ブラジル2026選挙フィルタに69アカウント追加（既定オン・即時実挙動変更）、(b) 「赤外線/サーモ映像を装った成人向けスパム動画」専用の Grok 検知フロー新設、(c) 広告の遅延コンバージョン学習の設計刷新（既定オフ）、(d) 不正対策サービスの Kafka マルチクラスタ化** |

---

## 非IT精通者向け・運用方法のまとめ（行動指針）

- **今日、実際に「今すぐ For You の並びが変わる」変更は1件だけです。ブラジルの政治アカウント除外リストが69件増えました（後述1）。** ランキングの計算式（`ScoringWeights`）の数値は**23日連続で据え置き**です。通常の日本語圏ユーザーの体感は、今日の差分だけでは変わりません。

- **① ブラジルの選挙関連アカウント除外リストが 2707件 → 2776件（+69件）になりました。これはスイッチで止められない、常時オンの処理です。**
  - X には、リストに載ったアカウントの投稿を **「そのアカウントをフォローしていない人の For You」から丸ごと落とす**フィルタがあります（`home-mixer/filters/brazil_2026_election_filter.rs:5632-5634`）。**フォローしている人のタイムラインには従来どおり出ます。**
  - 落とされる範囲は投稿者本人だけではありません。**リポスト元・引用元・返信ツリーの祖先**にリスト上のアカウントが1人でも含まれていれば、その候補は落ちます（同 `:5636-5650`）。つまり**「リスト上の政治家の投稿を引用した第三者の投稿」も、非フォロワーの For You から消えます。**
  - このフィルタは **フィーチャースイッチで無効化できません**。候補生成パイプラインに無条件で組み込まれています（`home-mixer/candidate_pipeline/phoenix_candidate_pipeline.rs:379`）。したがって**今日のリスト追加は、そのまま本番の挙動変更になります。**
  - **行動指針（ブラジル政治に言及するアカウント向け）：** ブラジルの選挙・政治家に関する投稿をリポスト/引用する運用は、**その相手がリストに入っていると自分の投稿ごとリーチを失います**。伸びない原因が自分の投稿内容ではなく「引用先」にある可能性があります。**日本語圏の一般利用にはまったく影響しません。**
  - なお、コメント欄の「アカウントが見つからない」リストから3件（`@TENENTEPEDROMIRANDA`、`@THEREZAFARIALIMA`、`@DRRICARDOBITENCOURT`）が消え、ID としても追加されていません。**追跡対象から外れただけ**と読めます（この3名は非フォロワーの For You にも再び出るようになります）。

- **② 「赤外線カメラ/サーモグラフィ映像に見せかけた成人向けスパム動画」を狙い撃ちする、専用の Grok 検知フローが新設されました（後述2）。今日いちばん大きい"新設"です。**
  - 何が行われるか：**動画付きの投稿**について、Grok の画像認識モデルに「**すでに違反と確定した動画のコマ割り（ストーリーボード）**」と**審査対象の投稿の動画**を並べて見せ、「**同じ手口か**」を判定させます（`grox/flows/ptos/task_safety_ptos_media_injected_adult_infrared_video_spam_detection.py:129-155`）。いわば**「前科のある動画との見比べ」**です。
  - **審査対象になる条件（4つすべて満たす投稿）**（`..._filter.py:32-45`）：
    1. 投稿者情報がある
    2. **投稿または引用元に動画が付いている**（画像のみ・テキストのみは対象外）
    3. **いいね数が 64 以上**（`grox/config/config.py:117` の `deluxe_fav_count_threshold` の既定値。**設定で変えられるため要確認**）
    4. **まだ NSFW 判定を受けていない**（既に成人向けフラグが付いた投稿は再審査しない）
  - **判定されると：** 投稿に **`SpamManipulatedMedia`（加工メディアによるスパム）** のポリシー違反注釈が付き、カテゴリは **`Spam`** になります（同 `:178-181`）。**「成人向けコンテンツ」ではなく「スパム」として扱われる**のが設計上のポイントです。
  - **使われるモデル：** `GROK_4_1_FAST_HEDGEHOG_CRITICAL`（同 `:67`）。1投稿あたり **60秒**のタイムアウト（同 `:46`）、**同一投稿は60秒間再判定しない**重複抑止付き（`grox/flows/ptos/task_rate_limit.py:33-38`）。
  - **「前科動画」の配布基盤も新設されました。** 違反確定済み動画のコマ割りを blobstore から取得し、**既定15分ごと**に更新する仕組みです（`grox/core/data_loaders/media_reference_bundle.py`、`grox/config/config.py:202-207`）。上限は**動画10本 × 各10コマ**（同 `media_reference_bundle.py:18-19`）。
  - **意味するところ（かみ砕くと）：** これは**「特定の1つの手口を、実例そのものを見本にして狩る」**という、極めて狭く鋭い対策です。従来の「ポリシー文章を読ませて判定させる」方式と違い、**手口の見本が更新されれば検知内容も即日で変わります**（コード変更なしで対象が変わる＝外から追跡できない）。
  - **クリエイター側の行動指針：**
    - **「サムネイルでは無害に見えるが、実際は成人向けの動画」という手口は、64いいね以上に伸びた時点で自動審査に入ります。** 赤外線/熱源映像風の加工は名指しで対象です。
    - **誤検知リスク：** **正当な赤外線・サーモグラフィ映像**（科学解説、野生動物の夜間撮影、建築の断熱診断、防犯カメラ映像など）は、**「見本の動画と見た目が似ている」**という理由で審査に掛かります。判定は Grok の裁量であり、**閾値や誤検知率はコードから読み取れません（要確認）**。この種の映像を投稿する方は、**投稿本文で用途・文脈を明示しておく**のが自衛策になります（判定には投稿本文も渡されます：同 `:144-147`）。
    - 違反とされた場合の分類が **「スパム」** であることに注意。スパム判定はアカウント全体のリーチ低下につながる系統の扱いです（この投稿からどう執行されるかはこのコードには含まれません：**要確認**）。
  - **なお、この機能が本番で動いているかはコードから断定できません。** 見本データの場所（`media_reference_bundles` の設定）が設定ファイル側にあり、**未設定なら毎回エラーになって何も判定されません**（同 `:74-76`）。**新設＝即稼働とは限りません（要確認）。**

- **③ 「返信スパム判定のログ文言」が修正され、判定系統の内訳が読み取れるようになりました（実挙動の変更ではありません）。**
  - これまで「1000フォロワー未満のバケット」と書かれていたログが、「**Gemma バケット**」に直されました（`grox/flows/reply_spam/task_spam_detection.py:46`）。**ログの文言だけの修正**で、閾値やモデルは変わっていません。
  - ただしこれで、**9/16・9/17 で報告した「Grok系とGemma系の分界点20万フォロワー」のうち、スコア0.0（＝スパム確定）を返す系統が Gemma 側**であることが裏付けられました。**行動への影響はありません。**

- **④ Grok の安全性判定で「ウェブ検索していい参照先」の選び方が変わりました（後述4）。**
  - Grok が投稿の真偽やスパム性を確かめるためにウェブ検索するとき、**許可ドメインが先頭5件までに切り捨て**られるようになりました（`grox/libs/grok_sampler/eapi_sampler.py:19,148`）。
  - あわせて、ドメインの並び順が変わりました。以前は「**投稿者のプロフィールURL**」が先頭に来ていましたが、今後は「**投稿本文のリンク → カードのリンク → 最後に投稿者プロフィールのリンク**」の順になります（`grox/core/data_loaders/data_types.py:721-734`）。重複排除も**順序を保つ方式**に変更されました（同 `:742-746`）。
  - **意味するところ：** **「その投稿が何を根拠に書かれているか」を、投稿者の自己紹介欄よりも優先して確認する**ようになりました。**リンクを多く貼る投稿では、6件目以降のドメインは検証に使われません。**
  - **行動指針：** **事実確認されたい根拠リンクは、投稿本文の早い位置に置く**のが有利です。逆に、**プロフィール欄に大量のURLを並べておく手法（判定を自分に有利なサイトへ誘導する狙い）は、今後は後回しにされて実質効かなくなります。** これは**スパム側の抜け道を1つ塞ぐ変更**です。
  - ※ この2つの変更が実際に連結して使われている確証は、公開コードの範囲では取れませんでした（`extract_domains` の呼び出し元が公開範囲外）。**連結は推定です（要確認）。**

- **⑤ 広告の「後から発生するコンバージョン」の学習方法が大きく作り替えられました（既定オフ・後述5）。今日いちばんの設計変更です。**
  - 広告をクリックした人が**その場では買わず、数日後に買う**ケースを、モデルがどう学習するかの設計です。今日、**遅延コンバージョン専用の10種類の予測ヘッド**（購入・カート追加・会員登録・決済開始など）が正式に学習対象として定義されました（`phoenix/xrex/data/recsys/ads_late_window.py:5-16`、`phoenix/xrex/data/recsys/constants.py:285-315`）。
  - **仕掛けの中核：** 遅延ヘッドは「**クリックはされたが、早い段階のコンバージョンは起きなかった**」サンプルだけで学習されます（`phoenix/xrex/models/ads_head_masking.py:50`）。**早期に成約したものを二重に数えない**ための設計です。
  - あわせて、これまで3つに分かれていた学習マスクの設定（`condition_conversion_on_click` / `train_view_through_heads` / `split_head_training_by_source`）が、**`ads_head_masking` という1つのスイッチに統合**されました（`phoenix/xrex/models/recsys_model.py:671`、`phoenix/xrex/models/ads_head_masking.py:27-51`）。**既定は False（オフ）**です。
  - **意味するところ（広告主・一般ユーザー双方）：** 有効化されれば、**「クリックから成約までが遅い商材」（高額品、検討期間の長いサービス、B2B）の広告が、これまでより正当に評価される**方向に働きます。裏返すと、**「その場で即成約する安価な商材」の相対的な優位が下がります。** 一般ユーザーから見ると、**タイムラインに出る広告の性質が「即買い系」から「じっくり検討系」へ寄る**可能性があります。
  - **ただし既定オフで、有効化の時期はコードから読み取れません（要確認）。** また、これは**学習（モデルの作り方）側の変更のみ**で、**配信側のロジック（`home-mixer/ads/`）は1行も変わっていません。今日の時点で広告の出方は変わりません。**

- **⑥ 広告と検索の新しい入力データの「配管」が敷かれました（後述6）。まだ使われていません。**
  - 広告に**時間窓ごとのエンゲージメント数**（例：直近1時間/6時間/24時間の反応数）を渡す枠が追加されました（`phoenix/.../recsys.proto:65-76,138,1475`）。オーガニック投稿側には既にある仕組みで、**それが広告にも来ます。**
  - 投稿に**検索語との一致度**を渡す枠が追加されました（`同 :1276-1282`）：**一致した単語の割合**と**完全一致フレーズか否か**の2つ。
  - **意味するところ：** 検索結果のランキングに「**キーワードがそのまま含まれているか**」という古典的な指標が、**明示的な入力として戻ってきます**。現在は意味ベースの埋め込み中心なので、**「検索語をそのまま含む投稿」が今後有利になる余地**ができました。**ただし枠を定義しただけで、まだ何にも使われていません（`.proto` 以外に参照が1件もない）。効くのは将来です。**

- **⑦ 不正対策サービス（自動執行）の基盤が、複数データセンター対応になりました（後述7）。ユーザー行動への直接影響はありません。**
  - スパム・不正の検知結果を受け取る仕組みを、**トピックごとに別のクラスタ/ゾーンから読める**ようになりました（`abuse-enforcement-service/service-lib/src/lib.rs:75-98`）。設定不備のトピックは**その1つだけスキップして他は動く**設計に変わりました（同 `:1840-1850`）。**取り締まりの止まりにくさ（可用性）を上げる変更**です。
  - **ただし要注意な副作用が2つあります（後述7）。** ①**すべてのトピックが設定不備でも、サービスは「正常」として稼働を続けます**（同 `:1899-1906`）。②**既定クラスタ以外のトピックのエラーは、自動復旧の見張り番（watchdog）の対象外**になりました（同 `:1335-1345,1851-1853`）。**検知データが届いていないのに執行サービスが健全に見える状態**が起こり得ます。これは**「取り締まりが静かに緩む」方向のリスク**です。ユーザーが今日体感する変化ではありません。

---

## 変更サマリ

1. **ブラジル2026選挙フィルタに69アカウント追加**（2707→2776件）。スイッチ不可の常時オン処理なので、**唯一の即時実挙動変更**。非フォロワーの For You から、本人・リポスト元・引用元・返信祖先まで含めて除外される。
2. **「赤外線/サーモ映像を装った成人向けスパム動画」専用の Grok 検知フロー新設。** 違反確定済み動画のコマ割りを"見本"として blobstore から15分ごとに配布し、動画付き・64いいね以上・未NSFW判定の投稿と見比べる。ヒットすると `SpamManipulatedMedia`（カテゴリ `Spam`）注釈。見本データの設定が必要なため稼働状況は不明。
3. **広告の遅延コンバージョン学習を刷新。** 遅延10ヘッドを「twin ヘッド」として定義し、3つに分散していたマスク設定を `ads_head_masking` に統合（既定オフ）。購入金額予測の指標に長窓EMA平滑化を追加。
4. **Grok のウェブ検索許可ドメインを先頭5件に制限**し、ドメイン優先順位を「投稿本文のリンク優先・投稿者プロフィール後回し」に変更。
5. **proto 拡張（配管のみ）：** 広告に時間窓別エンゲージメント数、投稿に検索語一致度（一致単語率・完全一致フレーズ）。
6. **不正対策サービスの Kafka マルチクラスタ対応。** 可用性向上だが、全消費者停止時も ready を返す／非既定クラスタのエラーが watchdog 対象外という2つの静かな緩みを持ち込んでいる。
7. **ランキング重み・広告配信・Thunder・OON係数・多様性・retrieval Top-K：すべて変更なし。** `param.rs` は同期タイムスタンプのみ。

---

## 変更点ごとの詳細（メカニズム → X ユーザー/クリエイターへの影響）

### 1. ブラジル2026選挙フィルタ：69アカウント追加（既定オン／即時実挙動変更）

**メカニズム（コード根拠）**

- 除外判定：`home-mixer/filters/brazil_2026_election_filter.rs:5632-5634`
  ```rust
  fn is_excluded_author(user_id: u64, followed_user_ids: &FxHashSet<u64>) -> bool {
      BRAZIL_2026_ELECTION_USER_IDS.contains(&user_id) && !followed_user_ids.contains(&user_id)
  }
  ```
  **リストに載っており、かつ閲覧者がフォローしていない**場合のみ除外。フォロー済みなら影響なし。
- 波及範囲：`同 :5636-5650` — `author_id` / `retweeted_user_id` / `quoted_user_id` / `ancestor_users`（返信ツリーの祖先）のいずれかがリストに該当すれば候補を落とす。
- 無効化不可：`home-mixer/candidate_pipeline/phoenix_candidate_pipeline.rs:379` で `Box::new(Brazil2026ElectionFilter)` として無条件登録。フィーチャースイッチのゲートがない。
- リスト規模：`同 :73`（`BRAZIL_2026_ELECTION_USER_IDS`）で **2776件**（今回 +69件、削除0件）。

**影響**

| 影響を受ける層 | 具体的な行動への影響 |
|---|---|
| 追加された69アカウント本人 | **本日から、フォロワー以外の For You に自分の投稿が一切出ません。** 既存フォロワーのタイムラインとプロフィール閲覧は従来どおり。リーチは「フォロワー数の天井」に固定されます |
| ブラジル政治を扱う第三者アカウント | **リスト上のアカウントをリポスト/引用/返信すると、その自分の投稿が非フォロワーの For You から落ちます。** 政治クリップ・切り抜き運用は、引用先がリストかどうかでリーチが激変します |
| ブラジルの一般閲覧者 | **フォローしていない政治アカウントの投稿は For You に出ません。** 政治情報はフォロー・検索・通知経由でのみ届きます |
| 日本語圏ユーザー | **影響なし**（リストはブラジルの政治家・候補者・選挙関連アカウントのみ） |

**影響度：中（ブラジル圏では大）** — 唯一の即時実挙動変更。

---

### 2. 「赤外線/サーモ映像を装った成人向けスパム動画」検知フロー新設

**メカニズム（コード根拠）**

- 新トピック・新フロー定義：`grox/flows/ptos/constants.py:34-39`、`grox/flows/ptos/generators.py:125-132`（Kafka トピック `safety-ptos-media-injected-adult-infrared-video-spam-detection` を購読）
- 処理順序：`grox/flows/ptos/plan_safety_ptos_media_injected_adult_infrared_video_spam_detection.py:22-44`
  `重複抑止 → 対象絞り込み → 動画のフレーム化 → Grok 判定 → 注釈の書き出し`
- 対象絞り込み（4条件）：`..._filter.py:32-45`
  - `no_user` / `no_video`（動画なしは除外）/ `not_high_fav`（`get_fav_count() < deluxe_fav_count_threshold`）/ `already_nsfw`
  - 閾値の既定値は **64いいね**（`grox/config/config.py:117`）
- 判定：`task_safety_ptos_media_injected_adult_infrared_video_spam_detection.py:129-155`
  - システムプロンプトに専用テンプレート（`grox/flows/ptos/prompts.py:60-64` が `media_injected_adult_infrared_video_spam_detection.j2` を参照）
  - ユーザーメッセージに「**確定違反動画のコマ割り × N本**」→「**審査対象の投稿（本文＋動画コマ）**」→「見比べて JSON で答えよ」
  - モデル：`GROK_4_1_FAST_HEDGEHOG_CRITICAL`（同 `:67`）、タイムアウト60秒（同 `:46`）
  - 出力は `<json>{"violation": bool, "reason": str}</json>` 形式（同 `:157-168`）
- 執行の注釈：`同 :169-184`
  ```python
  category=SafetyPolicyCategory.Spam,
  safetyPolicy=SafetyPolicy(policyType=SafetyPolicyType.SpamManipulatedMedia, ...)
  ```
  → **9/17 に受け皿として追加された `SpamManipulatedMedia` の最初の実利用**です。
- 見本データ（reference bundle）の配布：`grox/core/data_loaders/media_reference_bundle.py`
  - blobstore（`:15` エンドポイント）から `manifest.json` と各フレーム画像を取得（`:67-88`）
  - **上限：動画10本 × 各10コマ、1コマ2MB まで**（`:18-20,27,32`）
  - **既定15分ごと**に再取得、取得失敗時は前回分を使い続ける（`grox/config/config.py:206`、`media_reference_bundle.py:99-141`）
  - 未設定なら `None` を返し、タスク側は `RuntimeError`（`task...py:74-76`）→ **設定がなければ何も判定されません**
- 重複抑止：`grox/flows/ptos/task_rate_limit.py:33-38`（同一投稿を60秒 TTL でデデュープ、最大1万件）

**影響**

| 影響を受ける層 | 具体的な行動への影響 |
|---|---|
| 成人向けスパム投稿者（サムネ偽装型） | **64いいねに達した時点で自動審査に入ります。** 判定されると「成人向け」ではなく **「スパム」** 扱い。同じ動画の使い回しは見本に登録されて以後ほぼ確実に捕まります |
| 赤外線・サーモ映像を扱う正当なアカウント | **誤検知リスク圏に入りました。** 科学解説・野生動物の夜間撮影・断熱診断・防犯カメラ映像などは「見た目が見本に似ている」ことで審査対象になります。**投稿本文も判定材料に渡されるため、用途・文脈を本文に明記しておくのが自衛策です**（同 `:144-147`） |
| 一般閲覧者 | **動画付き投稿で64いいね以上に伸びたものだけが対象**なので、審査の物量は限定的。For You の体感は変わりません |
| 64いいね未満の動画投稿者 | **対象外**。小規模アカウントは（この検知に関しては）審査されません |

**影響度：中** — 対象が極めて狭い代わりに、**見本データを差し替えるだけでコード変更なしに検知対象を変えられる**構造。外部からの追跡が難しい点が重要です。

---

### 3. 広告の遅延コンバージョン学習：head masking の刷新（既定オフ）

**メカニズム（コード根拠）**

- 遅延ヘッド10種の定義：`phoenix/xrex/data/recsys/ads_late_window.py:5-16`
  `ADS_ATTRIBUTED_KEY_CLICK_CONVERSION_DELAYED` / `..._CLICK_..._DELAYED` / `PURCHASE` / `MID_FUNNEL` / `ADD_TO_CART` / `UPPER_FUNNEL` / `WEB` / `SEARCH` / `SIGN_UP` / `CHECKOUT_INITIATED`
- 対応する学習ラベルの列挙：`phoenix/xrex/data/recsys/constants.py:285-315`（`Is*ConversionDelayed` を10件追加）
- 「早期に成約したものは遅延ヘッドで学習しない」対応表：`ads_late_window.py:18-29`（`ADS_LATE_WINDOW_TWIN_SUPPRESSOR`）
- 3つのデータストリームの定義：`phoenix/xrex/models/ads_head_masking.py:18-20`
  `FRESH_STREAM_ID=0`（新鮮）/ `EARLY_RELABEL_STREAM_ID=1`（早期再ラベル）/ `LATE_SPLIT_STREAM_ID=2`（**新設・遅延窓**）
- マスクの中核：`同 :46-51`
  ```python
  engagement * fresh                        # 通常エンゲージメントは新鮮ストリームのみ
  + ct * early * click                      # クリック条件付きコンバージョンはクリック有りのみ
  + vt * early * (1 - click)                # ビュースルーはクリック無しのみ
  + twins * late * click * no_early_counterpart   # 遅延ヘッドは「クリック有り・早期成約なし」のみ
  ```
- 設定の統合：`phoenix/xrex/models/recsys_model.py:671`（`ads_head_masking: bool = False`）が、削除された `condition_conversion_on_click` / `train_view_through_heads` / `split_head_training_by_source` の3つを置き換え。`phoenix/xrex/configs/xrecsys.py:646` も追随
- 適用箇所：`recsys_model.py:3289-3291`（損失マスクに乗算）、新しい計測断面 `late_split_no_early`（`同 :1508-1510`）
- 購入金額予測の計測強化：`phoenix/xrex/models/loss_recsys.py:148-224`（**2^28・2^30 サンプルの長窓EMA**で loss / USD絶対誤差 / キャリブレーションを平滑化。`recsys_model.py:575`）。有効サンプルの条件も `sample_source > 0` → **`== EARLY_RELABEL_STREAM_ID`** に厳格化（`loss_recsys.py:110`）

**影響**

| 影響を受ける層 | 具体的な行動への影響 |
|---|---|
| 広告主（検討期間の長い商材） | **有効化されれば、成約までが遅い商材の広告が正当に評価される方向。** 高額品・B2B・不動産・教育などが有利化する余地 |
| 広告主（即買い系） | **相対的な優位が下がる方向**（遅延成約が正しく計上されるため） |
| 一般ユーザー | **有効化されれば、タイムラインの広告の性質が「即買い系」から「じっくり検討系」へ寄る可能性。** 今日の時点では何も変わりません |
| クリエイター（オーガニック） | **影響なし**（広告ヘッド専用のマスク） |

**影響度：小（現時点）／中（有効化時）** — `ads_head_masking` は既定オフ、`purchase_value_loss_weight` も既定 0.0。**配信側（`home-mixer/ads/`）は無変更なので、今日の広告の出方は変わりません。有効化の時期はコードから読み取れません（要確認）。**

---

### 4. Grok の安全性判定：ウェブ検索の参照先の選び方

**メカニズム（コード根拠）**

- 許可ドメインの上限：`grox/libs/grok_sampler/eapi_sampler.py:19`（`MAX_SEARCH_DOMAINS = 5`）、`同 :145-150`
  ```python
  if self.enable_search and search_allowed_domains:
      create_kwargs["tools"] = [web_search(allowed_domains=search_allowed_domains[:MAX_SEARCH_DOMAINS])]
  ```
- ドメイン抽出順序の変更：`grox/core/data_loaders/data_types.py:721-734`
  - **変更前：** 投稿URL → **投稿者プロフィールURL** → カードURL → 統合カードURL
  - **変更後：** 投稿URL → カードURL → 統合カードURL → **投稿者プロフィールURL（最後）**
- 重複排除を順序保持に：`同 :742-746`（`set` → `dict[str, None]`）。**先に来たドメインが優先的に5件枠に入る**ようになりました

**影響**

| 影響を受ける層 | 具体的な行動への影響 |
|---|---|
| リンクを多く貼る投稿者 | **6件目以降のドメインは事実確認に使われません。** 根拠リンクは本文の早い位置に置くのが有利 |
| プロフィール欄にURLを詰めるアカウント | **プロフィールのURLが最後尾に回り、5件枠に入りにくくなりました。** 「判定を自分のサイトへ誘導する」手法は実質的に効かなくなります（**抜け道を塞ぐ変更**） |
| 一般ユーザー | 直接の体感なし。**Grok の判定が「投稿の中身の根拠」に基づく方向に寄ります** |

**影響度：小** — ただしスパム側の回避手段を1つ潰す、方向性の明確な変更。
※ `extract_domains` の呼び出し元が公開範囲外のため、**2つの変更が実際に連結しているかは推定（要確認）。**

---

### 5. proto 拡張：広告のエンゲージメント数と検索語一致度（配管のみ）

**メカニズム（コード根拠）**

- 時間窓別エンゲージメント数のメッセージ新設：`phoenix/crates/serving/xai-recsys-proto/proto/recsys.proto:65-76`
  ```protobuf
  message EngagementCounts {
    bool present = 1;
    repeated uint32 window_secs = 2;      // 窓の長さ（秒）
    repeated EngagementCountRow rows = 3; // 行動種別ごとのカウント
    int64 updated_at_ms = 4;
    int64 window_end_ms = 5;
  }
  ```
- 2箇所に配線：`同 :138`（リクエストの `user_ads_engagement_counts` = **閲覧者側の広告反応履歴**）、`同 :1475`（`AdInfo.engagement_counts` = **広告側の反応実績**）
- 検索語一致度の新設：`同 :1276-1282`
  ```protobuf
  message SearchLexicalMatch {
    float matchedWordFraction = 1;  // 一致した単語の割合
    bool exactPhrase = 2;           // 完全一致フレーズか
  }
  ```
- Python 側 proto も同一内容（`phoenix/python/common/xai-proto/proto/recsys.proto:65-76,138,1276-1282,1475`）
- **既存の実装との接続：** オーガニック投稿側には既に `home-mixer/candidate_hydrators/engagement_counts_hydrator.rs` があり、`EnableEngagementCountsHydration` スイッチで制御されています。**それが広告にも拡張される形**です
- **利用箇所：** `SearchLexicalMatch` は `.proto` 以外に参照が **0件**。`user_ads_engagement_counts` も同様。**枠の定義のみ**

**影響**

| 影響を受ける層 | 具体的な行動への影響 |
|---|---|
| 検索流入を狙うクリエイター | **将来、「検索語をそのまま含む投稿」「検索語と完全一致するフレーズを含む投稿」が有利になる余地ができました。** 現在は意味ベースの埋め込み中心なので、これは古典的キーワードマッチの部分復権を示す設計 |
| 広告主 | **将来、広告に「直近の反応実績」（新しさ・勢い）が効くようになる余地。** 出したばかりで反応の薄い広告と、伸びている広告の扱いが分かれる方向 |
| 一般ユーザー | **今日の影響なし。** 参照0件の未使用フィールド |

**影響度：小（現時点）** — ただし**検索ランキングの方向性を示す重要な予兆**として追跡対象。

---

### 6. 学習バンドル出力：リクエスト由来とテンプレート由来の入力を明示分離

**メカニズム（コード根拠）**

- リクエストで実際に送られる入力キーの明示リスト新設：`phoenix/xrex/train/recsys_bundle_export.py:31-70`（`REQUEST_BATCH_KEYS`、36キー）
- リストに無いキーは**学習データのテンプレート値で定数埋め**され、`source: "template"` + `fill` 値として入力仕様に記録される（`同 :474-493`）。定数でなければ**ビルドが失敗**する（`同 :73-94`）
- 未登録かつテンプレート値も無いキーは**エラー**（`同 :480-484`）

**影響**

- **ユーザー行動への直接影響なし。** 「学習時には使っていたが推論時には送られてこない特徴量」が黙ってゼロ埋めされる事故を、**ビルド時に検出する**ための安全装置です。
- 副次的な読み取り：`REQUEST_BATCH_KEYS` に `candidate_seq.search_query_embeddings`、`candidate_seq.conversion_dense_features`、`candidate_seq.account_hashes` などが含まれており、**推論時に実際に送られている入力の一覧**が公開情報として読めるようになりました。

**影響度：小**

---

### 7. 不正対策サービス（abuse-enforcement-service）の Kafka マルチクラスタ化

**メカニズム（コード根拠）**

- トピックごとのクラスタ/ゾーン上書き：`abuse-enforcement-service/service-lib/src/lib.rs:75-98`（`TopicEntry` に `cluster` / `zone` を追加、空文字列は既定値扱い）
- 生産者側も同様：`abuse-enforcement-service/service-lib/src/growthbook.rs:54-56`、`lib.rs:705-707`。設定が壊れているトピックは**警告を出してそのシンクだけ無効化**（`growthbook.rs:231-237`）
- mTLS 証明書の読み込みを事前に一度だけ実施し、接続構築を `for_cluster_mtls_zone` に変更（`lib.rs:1557-1558`, `同 :1815-1821`）
- 起動失敗の個別スキップ＋メトリクス：`lib.rs:1840-1854`（`abuse_enforcement_kafka_consumer_start_total{topic,cluster,result}`、`abuse-enforcement-service/service-lib/src/metrics.rs:170-175`）
- **注意点①：全消費者が起動失敗しても ready を立てる**：`lib.rs:1899-1906`
  ```rust
  if handles.is_empty() {
      if topics_len > 0 { error!("no Kafka consumers started ... serving without consumers"); }
      state.kafka_ready.store(true, Ordering::Relaxed);
  }
  ```
  → **エラーログとメトリクスは出るが、ヘルスチェックは通ります。**
- **注意点②：watchdog のエラー監視が既定クラスタのトピックに限定**：`lib.rs:1335-1345`（`consumer_error_total` が `watched` トピックのみ集計）、`同 :1851-1853`（既定クラスタ/ゾーンのトピックだけを `error_topics` に登録）
  → **上書きクラスタのトピックがエラーを出し続けても、自動復旧（self-delete）は発動しません。**
- 事前接続確認（preflight）も既定クラスタのトピックのみ：`lib.rs:1561-1580`、`同 :1959-1966`。該当が無ければスキップし、その旨をログに出す

**影響**

| 影響を受ける層 | 具体的な行動への影響 |
|---|---|
| 一般ユーザー・クリエイター | **今日の直接影響なし。** ただし**取り締まりの安定性**に関わります |
| （リスク） スパム・不正アカウント | **設定不備や非既定クラスタの障害時に、検知データが届かないまま執行サービスが「健全」に見える状態**が起こり得ます。**「自動執行が静かに緩む」方向のリスク**です |

**影響度：小（ユーザー行動へは間接的）** — ただし**運用上のリスクを2つ持ち込んでいる**点は記録しておく価値があります。

---

### 8. その他の小変更

- **返信スパム判定のログ文言修正**：`grox/flows/reply_spam/task_spam_detection.py:46`。「1000フォロワー未満のバケット」→「Gemma バケット」。**実挙動の変更なし。** ただし **スコア0.0（スパム確定）を返すのは Gemma 系統**であることの裏付けになりました。
- **`visibility-filtering/hydration/gizmoduck_hydrator.rs:176`, `:310`**：テストコードに `..Default::default()` を2箇所追加。**テストのみ。実挙動なし。**
- **`phoenix/Cargo.toml:69` / `Cargo.lock`**：`thrift` の取得元を GitHub の git タグ（`v0.24.0`）から crates.io の公開版 `0.24.0` に切り替え。**バージョンは同一で、実挙動の変更はありません。**
- **`home-mixer/params/param.rs:1`**：同期タイムスタンプのみ（`2026-09-16T16:23:12Z` → `2026-09-17T20:46:44Z`）。**フィーチャースイッチの既定値は1つも変わっていません。**

---

## 影響度評価まとめ

| # | 変更 | 既定 | 影響度 | 影響を受ける層・行動 |
|---|---|---|---|---|
| 1 | ブラジル選挙フィルタ +69件（計2776件） | **オン（無効化不可）** | **中**（ブラジル圏は**大**） | 追加69アカウント／その投稿を引用・リポストする第三者／ブラジルの一般閲覧者。**日本語圏は影響なし** |
| 2 | 赤外線/サーモ偽装の成人向けスパム動画検知（新設） | 設定依存（要確認） | **中** | 動画付き・64いいね以上・未NSFW判定の投稿。**正当な赤外線/サーモ映像の投稿者に誤検知リスク** |
| 3 | 広告の遅延コンバージョン学習の刷新 | **オフ** | 小（有効化時：**中**） | 広告主（検討期間の長い商材が有利化する方向）／広告の質を通じて一般ユーザー |
| 4 | Grok 検索許可ドメイン上限5件＋順序変更 | オン | **小** | リンク多用の投稿者／プロフィールURLで判定誘導するスパム（抜け道が塞がる） |
| 5 | proto：広告エンゲージメント数・検索語一致度 | 未使用 | 小（将来：**中**） | 将来、検索流入狙いのクリエイター／広告主 |
| 6 | 学習バンドル入力の明示分離 | オン（ビルド時検査） | 小 | ユーザー影響なし（安全装置） |
| 7 | 不正対策サービスの Kafka マルチクラスタ化 | オン | 小（間接） | 全ユーザー（取り締まりの安定性。**静かに緩むリスクあり**） |
| 8 | ログ文言・テスト・依存更新 | — | なし | — |

**ランキング重み（`ScoringWeights`）は23日連続で数値変更なし。** Thunder リテンション窓・OON係数・著者多様性の decay/floor・retrieval Top-K・広告挿入密度・ブランドセーフティも**すべて無変更**。

---

## 不確実な点（要確認）

1. **新設の赤外線/サーモ動画スパム検知が本番稼働しているか。** 見本データの場所は `grox_config.media_reference_bundles["media_injected_adult_infrared_video_spam"]` として**設定ファイル側**にあり（`grox/config/config.py:219-221`）、公開コードには含まれていません。**未設定なら 1件も判定されません**（`task...py:74-76`）。
2. **判定プロンプトの内容が公開されていません。** `media_injected_adult_infrared_video_spam_detection.j2` テンプレートはリポジトリに含まれておらず（`.j2` ファイルが1件も存在しない）、**どのような基準で「違反」と判定されるかは読み取れません。** 誤検知率・閾値も不明。
3. **見本データの中身は追跡できません。** blobstore 上の動画コマ割りは外部から見えず、**コード変更なしに検知対象が変わります**（15分ごとに更新）。「今日のコードでは無害だった投稿が明日は違反になる」ことがあり得ます。
4. **`deluxe_fav_count_threshold` の本番値。** コード上の既定値は 64（`grox/config/config.py:117`）ですが、設定で上書き可能です。本番の実効閾値は不明。
5. **`SpamManipulatedMedia` 判定後の執行内容。** 注釈を書き出すところまでが公開範囲で、**その注釈がリーチ低下・ラベル表示・削除のどれにつながるかは含まれていません。**
6. **`ads_head_masking` と `purchase_value_loss_weight` の有効化時期。** どちらも既定オフ（False / 0.0）で、有効化の予定・時期はコードから読み取れません。
7. **`SearchLexicalMatch` と `user_ads_engagement_counts` の利用開始時期。** `.proto` 以外に参照が0件で、いつ・どのモデルで使われるかは不明。
8. **Grok のドメイン抽出順序変更と検索上限5件の連結。** `Post.extract_domains()` の呼び出し元が公開範囲外のため、**この2変更が同一経路で効いているかは推定**です。
9. **追跡対象から外れた3アカウント**（`@TENENTEPEDROMIRANDA`、`@THEREZAFARIALIMA`、`@DRRICARDOBITENCOURT`）が、意図的な除外か単なるリスト整理かはコードから判断できません。
10. **不正対策サービスの「全消費者停止でも ready」挙動が意図的な設計か**、フェイルオープンの見落としかは判断できません。エラーログとメトリクス（`result="config_error"`）は出るため、**監視前提の意図的な設計である可能性が高い**と読めます。
